{
  "buildCommand": "npm run build",
  "devCommand": "vite --host 127.0.0.1 --port $PORT",
  "outputDirectory": "dist",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}

